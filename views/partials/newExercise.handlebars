<div class="exercise">
    <div class="w-full md:w-[60%] p-2 bg-secondary text-center">
        <div class="font-bold text-[1.75rem]">{{exercise.name}}</div>
        <a href="`{{exercise.gif_url}}`" class="underline text-gray-300">Learn Movement</a>
    </div>    
    <div onclick = "handlePlusClick({{exercise}}, {{routine.id}})"class="addColumn text-[2rem] bg-primary w-fit md:w-[10%] h-fit flex justify-center items-center rounded-full p-3">
        <img class="addImg h-[2rem] cursor-pointer" src="./images/add.svg" alt=""/>
    </div>
</div>
<script>
    column2.addEventListener("click", (e) => {
        e.preventDefault();
        console.log("hi");
        postExercise({{exercise}}, {{routine.id}});
        buildList(workoutData);
      });
</script>